on:
  [push]
jobs:
  auth-and-run:
    runs-on: ubuntu-latest
    permissions:
      id-token: write

    steps:
      - uses: Yharnix/ci/.github/worflows/issue_credentials.yml@master
        with:
          workflow_ref: '$GITHUB_WORFLOW'

      - name: Run AWS CLI commands
        run: |
          aws sts get-caller-identity
          aws s3 ls
